import sys


def merge(A, p, q, r):
    return

if __name__ == '__main__':
    input = sys.stdin.readline
    A = list(map(int, input().split()))
    p = 0
    q = len(A) // 2 + 1
    r = list()

    merge(A , p, q, r)